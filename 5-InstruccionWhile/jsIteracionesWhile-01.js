/*
al presionar el botón mostrar 10 repeticiones 
con números ASCENDENTE, desde el 1 al 10.*/
function mostrar()
{
	/** 
	contador: variable mas una constante
	 contador = contador + 1
	 contador += 1;
	 contador++; post incremento = 
	 ++contador; pre incremento = primero untiliza el valor y luego lo aumenta

	 acumulador: variable mas otra variable
	 acumulador = acumulador + otraVariable
	 acumulador += otraVariable
	 */

	var contador = 0;

	while(contador < 10) {
		contador++
		console.log(contador);
	}
	
}//FIN DE LA FUNCIÓN